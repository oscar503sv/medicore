"""Domain layer — the framework-agnostic core of Medicore.

Contains entities, value objects, enums, domain services and repository interfaces (ports).
This package must not import from application, infrastructure or presentation.
"""
